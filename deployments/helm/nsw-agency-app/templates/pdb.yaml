{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "nsw-agency-app.fullname" . }}
  labels:
    {{- include "nsw-agency-app.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "nsw-agency-app.selectorLabels" . | nindent 6 }}
{{- end }}
